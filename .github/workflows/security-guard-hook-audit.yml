name: Security Guard Hook Audit
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 12 * * 1'
jobs:
  hook-audit:
    uses: KooshaPari/phenoShared/.github/workflows/security-guard-hook-audit-reusable.yml@72b9c6cbdb24c49189b0e7c7395d874830d1ed87
