name: Security Guard Hook Audit

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 12 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  hook-audit:
    uses: KooshaPari/phenoShared/.github/workflows/security-guard-hook-audit-reusable.yml@4b820a6d1eca7884e692e4ec4edf2b59732809cd
