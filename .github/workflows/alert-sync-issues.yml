name: Alert Sync Issues

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  security-events: read

jobs:
  alert-sync:
    uses: KooshaPari/phenoShared/.github/workflows/alert-sync-issues-reusable.yml@80b1198577542f0e4d565f7bccdf495e50e630c8
    with:
      auto-label: auto-alert-sync
