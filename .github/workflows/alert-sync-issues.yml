name: Alert Sync Issues

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
timeout-minutes: 10

permissions:
  contents: read
  issues: write
  security-events: read

jobs:
  alert-sync:
    uses: KooshaPari/phenoShared/.github/workflows/alert-sync-issues-reusable.yml@4b820a6d1eca7884e692e4ec4edf2b59732809cd
    with:
      auto-label: auto-alert-sync

