name: Alert Sync Issues

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  security-events: read

jobs:
  alert-sync:
    uses: KooshaPari/phenoShared/.github/workflows/alert-sync-issues-reusable.yml@ac1fef63f31acb3ec550bfdc1f1915075dd1fbb9
    with:
      auto-label: auto-alert-sync
