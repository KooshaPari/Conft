name: Trufflehog Secrets Scan
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
  pull_request:

timeout-minutes: 10
permissions:
  contents: read

jobs:
  trufflehog:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - uses: actions/setup-go@0a12ed9e1a4ce4b1a02a5f2dd1e3a9c9e6c7f8b1 # v5.13.0
        with:
          go-version: 'stable'
      - run: go install github.com/trufflehog/trufflehog/v3@v3.95.3
      - run: trufflehog github --only-verified --no-update
        env:
          GH_TOKEN: ${{ github.token }}
