name: CI
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  ci:
    uses: KooshaPari/phenoShared/.github/workflows/ci-reusable.yml@4b820a6d1eca7884e692e4ec4edf2b59732809cd
    with:
      language: typescript
