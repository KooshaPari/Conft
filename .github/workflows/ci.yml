name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: KooshaPari/phenoShared/.github/workflows/ci-reusable.yml@72b9c6cbdb24c49189b0e7c7395d874830d1ed87
    with:
      language: typescript
