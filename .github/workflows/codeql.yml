name: CodeQL
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "23 4 * * 2"
  workflow_dispatch:

timeout-minutes: 20
permissions:
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze JavaScript
    runs-on: ubuntu-24.04
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@d632683dd7b4114ad1992be3c231f06b3cb7b4de # v6.0.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@eb055d739abdc5e59edeced7979711f62af6b830 # v3
        with:
          languages: javascript

      - name: Autobuild
        uses: github/codeql-action/autobuild@eb055d739abdc5e59edeced7979711f62af6b830 # v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@eb055d739abdc5e59edeced7979711f62af6b830 # v3
        with:
          category: "/language:javascript"
