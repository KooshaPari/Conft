name: Security Audit

on:
  push:
    paths:
      - 'typescript/packages/conft/package.json'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - uses: actions/setup-node@8f152de45cc393bb48ce5d89d36b731f54556e65
      - run: cd typescript/packages/conft && npm audit --audit-level=moderate
        continue-on-error: true
